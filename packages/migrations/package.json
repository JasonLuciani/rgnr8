{
  "name": "@rgnr8/migrations",
  "version": "0.1.0",
  "description": "RGNR8 forward-only SQL migration runner: ordered, idempotent, checksum drift-detecting schema versioning over a minimal Postgres-compatible seam.",
  "type": "module",
  "main": "dist/index.js", "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": { "build": "tsc -p tsconfig.build.json", "typecheck": "tsc -p tsconfig.json --noEmit", "test": "node --import tsx --test test/*.test.ts" },
  "license": "UNLICENSED", "private": true,
  "devDependencies": { "@types/node": "^22.20.1", "pg-mem": "^3.0.5", "tsx": "^4.19.2", "typescript": "^5.6.3" }
}
