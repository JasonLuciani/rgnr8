{
  "name": "@rgnr8/prototype",
  "version": "0.1.0",
  "description": "End-to-end RGNR8 prototype harness: QBO import → ledger → close → sealed package (TS), read back + forecast + briefing + web + delivery (Python).",
  "type": "module",
  "private": true,
  "scripts": { "seed": "node --import tsx seed.ts" },
  "license": "UNLICENSED",
  "dependencies": {
    "@rgnr8/ledger-kernel": "^0.1.0",
    "@rgnr8/statements": "^0.1.0",
    "@rgnr8/close": "^0.1.0",
    "@rgnr8/qbo-migrate": "^0.1.0"
  },
  "devDependencies": { "@types/node": "^22.20.1", "tsx": "^4.19.2", "typescript": "^5.6.3" }
}
